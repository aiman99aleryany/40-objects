// Create an object literal called car with two properties: name and color. Print in the console the values of the properties.

let car = {
  name: "Aston Martin",
  color: "Grey",
};

console.log(car.name);
console.log(car.color);
